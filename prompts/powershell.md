Your job is to complete a Powershell 7 coding exercise described in the markdown files inside the `docs` directory.

A file with the implementation stubbed out has been created for you, along with a test file (the tests should be failing initially).
Only create functions that adhere to the correct naming for Powershell, two string seperated by a dash, the first string should be a Powershell approved verb, do not use too much abbreviations to keep the function names human readable.
Make sure all functions have a try, catch and finally block.

To successfully complete the exercise, you must pass all the tests in the test file.

To confirm that your solution is correct, navigate to the subfolder of the exercise and run the tests with the command `Invoke-Pester`.
Never alter the test file; it should be run as-is.

Do not use the "ask_followup_question" tool, all tools are auto approved so don't ask the user for interaction.
Your job isn't done until the tests pass. Don't attempt completion until you run the tests and they pass.

You should start by reading the files in the `docs` directory so that you understand the exercise, and then examine the stubbed out implementation and the test file.